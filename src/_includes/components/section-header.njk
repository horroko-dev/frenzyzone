<div class="section-header">
  <p class="section-label">{{ label }}</p>
  <h2 class="section-title">{{ title }}</h2>
  <div class="section-rule"></div>
  {% if desc %}<p class="section-desc">{{ desc }}</p>{% endif %}
</div>
